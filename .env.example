# Dungeon Development Configuration
# Copy this file to .env and customize: cp .env.example .env
#
# IMPORTANT: .env file is gitignored and safe for local settings.
# This example shows all available options with their defaults.

# ============================================================
# Backend Selection (Just command runner)
# ============================================================

# ZK backend to use by default with Just commands
# Options: risc0, stub, sp1, arkworks
# Default: risc0
# ZK_BACKEND=stub

# ============================================================
# Runtime Features
# ============================================================

# Enable zero-knowledge proof generation
# WARNING: First build is slow (30-60s for RISC0 guest compilation)
# Default: false
# ENABLE_ZK_PROVING=true

# Enable game state persistence (checkpoints, action logs, event logs)
# Automatically enabled if ZK proving is enabled
# Default: false
# ENABLE_PERSISTENCE=true

# ============================================================
# Persistence Configuration
# ============================================================

# Session identifier for save files
# Used to organize saves: {save_dir}/sessions/{session_id}/
# Default: auto-generated timestamp (e.g., session_1234567890)
# GAME_SESSION_ID=my_session

# Directory for save data (checkpoints, logs, proofs)
# Default:
#   macOS:   ~/Library/Application Support/dungeon
#   Linux:   ~/.local/share/dungeon (or $XDG_DATA_HOME/dungeon)
#   Windows: %APPDATA%\dungeon
# Override for development (use local directory):
# SAVE_DATA_DIR=./save_data

# Number of actions between automatic state checkpoints
# Lower = more frequent saves, higher storage usage
# Higher = less frequent saves, faster but riskier
# Default: 10
# CHECKPOINT_INTERVAL=20

# ============================================================
# Content Loading
# ============================================================

# Override content data directory (maps, actors, items, loot tables)
# Useful for testing custom content without modifying source
# Default: auto-detected from executable location or current directory
# CONTENT_DATA_DIR=./custom_content

# ============================================================
# Logging
# ============================================================

# Log level filter (error, warn, info, debug, trace)
# Default: info
# RUST_LOG=info

# Module-specific logging (comma-separated)
# Examples:
#   Show debug logs for runtime workers only:
#   RUST_LOG=runtime::workers=debug,info
#
#   Show trace for specific modules:
#   RUST_LOG=runtime::simulation=trace,game_core=debug,info
#
# RUST_LOG=runtime::workers=debug,game_core=info

# ============================================================
# RISC0 ZK Backend Settings
# ============================================================

# Skip RISC0 guest binary builds during compilation
# Useful for fast iteration when not modifying guest code
# Set by CI and Just commands automatically
# Default: not set
# RISC0_SKIP_BUILD=1

# Enable RISC0 development mode (faster proofs, not production-ready)
# WARNING: Only for testing, not cryptographically secure
# Default: not set
# RISC0_DEV_MODE=1

# ============================================================
# Advanced: Runtime Tuning
# ============================================================

# NOTE: These are advanced settings. Defaults work well for most cases.
# Only change if you understand the performance implications.

# Action queue buffer size (for async runtime channels)
# Default: 10
# CLI_ACTION_BUFFER=20

# Message log capacity (in-game message history)
# Default: 64
# CLI_MESSAGE_CAPACITY=100
