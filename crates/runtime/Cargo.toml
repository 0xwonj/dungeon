[package]
name = "runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
game-core = { path = "../game/core" }
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
tracing = "0.1"
thiserror = "1"
